#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/Users/kames/workspace/soulcialites/web3-citizen/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="$NODE_PATH:/Users/kames/workspace/soulcialites/web3-citizen/node_modules/.pnpm/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../node_modules/.pnpm/tsdx@0.14.1_@types+node@18.0.0/node_modules/tsdx/dist/index.js" "$@"
else
  exec node  "$basedir/../../../../node_modules/.pnpm/tsdx@0.14.1_@types+node@18.0.0/node_modules/tsdx/dist/index.js" "$@"
fi
