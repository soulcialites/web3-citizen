export declare function createDelegation(to: string, verifyingContract: string): {
    delegation: {
        delegate: string;
        authority: string;
        caveats: never[];
    };
    string: string;
};
